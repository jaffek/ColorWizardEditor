package com.affek.colorwizardeditor.image_picker.data.repository

import android.net.Uri
import javax.inject.Inject

class ImagePickRepository @Inject constructor() {

    suspend fun saveImage(uri : Uri?): Unit {

    }
}